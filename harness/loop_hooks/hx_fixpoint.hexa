// L7_FIX — P5 fixpoint 재검증 (v2 == v3 IDENTICAL)
// 본체:
//   1. self/native/hexa_v2 로 hexa_v3 재생성
//   2. v2 vs v3 바이너리 diff
//   3. IDENTICAL 아니면 REJECT (fixpoint 파괴)
fn main() {
    println("L7_FIX: hexa-lang P5 fixpoint stub — not yet implemented")
}
