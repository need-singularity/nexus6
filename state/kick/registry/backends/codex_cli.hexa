# state/kick/registry/backends/codex_cli.hexa
# kick(ω-cycle) backend registry — codex CLI slot (REAL adapter, post-probe).
# Format: indented key/value DSL (parsed by tool/kick_tree.hexa).
#
# OBSTRUCTION obs_1 RESOLVED 2026-04-26 — codex CLI probed and found installed:
#   probe: which codex   → /opt/homebrew/bin/codex
#   probe: codex --version → codex-cli 0.121.0
#   probe: codex exec --help → non-interactive subcommand confirmed (positional [PROMPT])
#                              flags: -c key=value, -m <MODEL>, -s <SANDBOX_MODE>, --full-auto,
#                                     --dangerously-bypass-approvals-and-sandbox, -i <FILE>...
#   install hint (if missing on other host): brew install codex   # or download from
#     https://github.com/openai/codex
# Adapter shape mirrors backends/claude_code.hexa per architecture obs_1 step_3 reframe.

backend 2 active "codex cli — OpenAI Codex CLI in non-interactive exec mode (probed 2026-04-26: /opt/homebrew/bin/codex codex-cli 0.121.0)"
  slug codex_cli
  cli-binary codex
  invocation exec --full-auto
  prepare-prompt-fn _kick_prompt_codex_cli
  spawn-fn _kick_spawn_codex_cli
  stream-fn _kick_stream_codex_cli
  capture-witness-fn _kick_capture_witness_codex_cli
  teardown-fn _kick_teardown_codex_cli
  cost-estimate-fn _kick_cost_codex_cli
  sentinel-format __KICK_RESULT__ <PASS|FAIL> witness=<path> tier1=<N> falsifier_pass=<N>
  err-trailer reason=<slug> fix=<directive>
  installed true
  installed-path /opt/homebrew/bin/codex
  installed-version codex-cli 0.121.0
  probe-ts 2026-04-26T18:30:00+09:00
  evidence-source carry-forward-cycle 2026-04-26_kick_carry_forward_omega_cycle (which codex + codex --help + codex exec --help)
  obs1-status RESOLVED
  obs1-reframe registry slot filled with real adapter shape; codex exec is the non-interactive entry point analogous to claude -p
