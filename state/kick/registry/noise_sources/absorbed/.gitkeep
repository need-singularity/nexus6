# empty — populated by tool/kick_dispatch.hexa witness_emit on each cycle.
# Each Tier-1 promotion lands as state/kick/registry/noise_sources/absorbed/<witness_id>_<tier1_slug>.hexa
# (NEXUS Absorb stage — feeds α absorbed-primitive sampling on subsequent kicks).
