# state/kick/registry/strata/surge.hexa
# kick(ω-cycle) stratum registry entry — L4 super-orchestrator (surge).
# Format: indented key/value DSL (parsed by tool/kick_tree.hexa).

stratum 4 active "L4 surge — Cartesian product engines × variants × seeds fan-out"
  slug surge
  level L4
  description super-orchestrator — Cartesian product (engines × variants × seeds), capped by NEXUS_SURGE_MAX (default 12)
  dispatch-fn cmd_surge
  registry-of-record state/kick/registry/strata/surge.hexa
  evidence-source cli/run.hexa cmd_surge @4136
