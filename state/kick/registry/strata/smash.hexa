# state/kick/registry/strata/smash.hexa
# kick(ω-cycle) stratum registry entry — L1 atomic single-phase blowup.
# Format: indented key/value DSL (parsed by tool/kick_tree.hexa).
# raw 45 SSOT pattern; raw 47/48 multi-axis-orthogonal mandate.

stratum 1 active "L1 atomic single-phase blowup — single-shot blowup 9-phase 돌파 (no iteration)"
  slug smash
  level L1
  description atomic single-phase blowup; one-shot dispatch into blowup engine; no rounds
  dispatch-fn cmd_smash
  registry-of-record state/kick/registry/strata/smash.hexa
  evidence-source design/abstraction_ceiling.md §1 (drill ladder L1-L_ω)
