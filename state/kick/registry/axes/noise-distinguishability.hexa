# state/kick/registry/axes/noise-distinguishability.hexa
# kick(ω-cycle) axis registry — noise-distinguishability (this cycle, F13-driven).
# Format: indented key/value DSL (parsed by tool/kick_tree.hexa).

axis 4 active "noise-distinguishability axis — every outside-noise injection hash-anchored + source-tagged"
  slug noise-distinguishability
  description every noise injection envelope = {mechanism, source_id, byte_hash, ts, payload_summary}; audit-time inside/outside-the-well distinction is mechanical
  lint-fn tool/kick_dispatch.hexa#envelope-shape
  correlation-budget 0.7
  registry-of-record state/kick/registry/axes/noise-distinguishability.hexa
  raw-anchor architecture-cycle F13 noise-distinguishability
  envelope-field mechanism
  envelope-field source_id
  envelope-field byte_hash
  envelope-field ts
  envelope-field payload_summary
