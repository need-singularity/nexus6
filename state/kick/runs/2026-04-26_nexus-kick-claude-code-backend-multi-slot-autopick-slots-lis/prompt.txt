# kick(ω-cycle) — topic: nexus kick claude_code backend multi-slot autopick + slots-list JSON 구현. 컨텍스트: claude code CLI 계정 다수 (~/.claude-claude<N>), 기본 슬롯이 5h/주간 limit 자주 걸림. 작업: (1) tool/claude_slot_pick.hexa standalone helper — slot pool discovery (phantom guard: 빈 N expansion 으로 ~/.claude-claude mkdir 방지), 슬롯별 cooldown 캐시 (~/.claude-claude<N>/.kick_limit_until), pick policy (LRU 또는 round-robin — 결정해 docs 화), CLAUDE_CONFIG_DIR env 주입 (2) kick_dispatch claude_code path 가 helper 경유, limit-hit stream-json 시그널 캐치 → cooldown 마킹 + 다음 슬롯 fallback, 전 슬롯 소진 시 raw 66 reason+fix hard-fail (3) nexus kick slots [--json] [--refresh] [--include-phantom] 서브커맨드 — JSON 스키마: schema_version/generated_at/slots[{slot,config_dir,status:available|session_limit|weekly_limit|unknown|phantom,session:{limit_until,remaining_5h_window_pct},weekly:{limit_until,remaining_pct,renews_in_days},last_used,last_probe,probe_source:cache|live_probe|stream_json_signal}]/summary, --json 은 stdout-only (로그 stderr), 추정값 금지 (null). codex_cli/gemini_cli backend 무변. 2-cycle: architecture cycle (slot-policy/cooldown 메커니즘/probe source 결정) → code-emission cycle (helper + dispatch wiring + slots subcommand + registry 업데이트). Falsifiers: empty-glob phantom 안만든다 / cooldown 슬롯 skip / 전소진 hard-fail / limit-hit auto-fallback / codex_gemini 무변 / 빈 pool valid JSON exit0 / --json stdout-only / phantom default 숨김 / probe_source null 시 추정값 없음. raw 80 sentinel + raw 66 err + git-hook 금지(raw#13) + nexus add+commit 단일 chain.
# stratum: drill
# axes: design-strategy,falsifier-coverage
# witness landing path: /Users/ghost/core/nexus/design/kick//2026-04-26_nexus-kick-claude-code-backend-multi-slot-autopick-slots-lis_omega_cycle.json
# noise envelopes (raw 47 cross-repo-trawl-witness; raw 80 sentinel format):
#   alpha: {"mechanism":"alpha","source_id":"absorbed:2026-04-26_autoabsorbsmoke_synthetic_omega_cycle__autoabsorbsmoke_gamma","byte_hash":"994d830420d14e00","ts":"2026-04-26T11:47:38Z","payload_summary":"absorbed_pick=2026-04-26_autoabsorbsmoke_synthetic_omega_cycle__autoabsorbsmoke_gamma from 5 primitives"}
#   beta : {"mechanism":"beta","source_id":"urandom","byte_hash":"c274fccf2c55c25b","ts":"2026-04-26T11:47:38Z","payload_summary":"qrng_byte=78 source=urandom (obs_2 reframe — urandom-fallback witnessed)"}
#   gamma: {"mechanism":"gamma","source_id":"bridge:bridge_uniprot","byte_hash":"f404b9fba028d9fc","ts":"2026-04-26T11:47:38Z","payload_summary":"bridge_pick=bridge_uniprot invocation=tool/uniprot_bridge.hexa (real fetch deferred to sub-agent or OFFLINE-FALLBACK)"}
# core 5-op kernel (architecture Tier-1):
#   1) intake topic
#   2) pre-register falsifiers
#   3) fan out K≥3 orthogonal axes (raw 48 correlation < 0.7)
#   4) gate Tier-1 promotion via falsifier-pass-pattern
#   5) emit witness JSON to declared landing path
#
Fire kick(ω-cycle) on the topic above. Land witness JSON at the declared path.
On completion print the sentinel line: __KICK_RESULT__ <PASS|FAIL> witness=<path> tier1=<N> falsifier_pass=<N>
On error print: reason=<slug> fix=<directive>
