// AUTO-GENERATED — shared/calc/alm_verify/generator.hexa
// manifest line id: alm_r12_eval_benchmark_fixed_set
// round: r12  gate: G_EVAL  verifier_type: benchmark_fixed_set
// hyp: 100-prompt fixed benchmark kr_gen match >= 80%
//
// Deterministic — LLM judge 금지.

let id = "alm_r12_eval_benchmark_fixed_set"
let target = "0.80"
let dataset = "shared/bench/alm_kr_gen_100.jsonl (NEW, to build)"

// TBD — fixed benchmark dataset not yet curated (Tier B in design doc §10).
// Contract when ready: load dataset, iterate prompts, compare actual signature
// vs expected (exact or tolerance). Emit PASS iff match_count/N >= target.
println("verify: " + id)
println("  dataset:  " + dataset)
println("  target:   " + target)
println("  status:   SKIP (benchmark dataset not yet built)")
println("__VERIFY_RESULT__ " + id + " SKIP 0 vs " + target)
// SKIP exits 0 — gate-level aggregator treats SKIP as pending, not FAIL.
