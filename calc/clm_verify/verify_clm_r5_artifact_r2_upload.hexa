// AUTO-GENERATED — shared/calc/clm_verify/generator.hexa
// manifest line id: clm_r5_artifact_r2_upload
// round: r5  gate: G_ARTIFACT  verifier_type: r2_object_exists
// hyp: ckpt uploaded to R2
//
// Deterministic — LLM judge 금지.

let id = "clm_r5_artifact_r2_upload"
let target = "true"
let r2_path = "r2:anima-models/clm170m/r5/"

let pod = env("HEXA_VERIFY_POD")
let pod_host = if pod != "" { pod } else { "87xsc" }
let cmd = "ssh " + pod_host + " 'rclone ls " + r2_path + " 2>/dev/null | wc -l'"
let count = to_int(exec(cmd).trim())
let status = if count >= 1 { "PASS" } else { "FAIL" }
let count_str = to_string(count)
println("verify: " + id)
println("  r2 path:  " + r2_path)
println("  objects:  " + count_str)
println("  target:   >= 1")
println("  status:   " + status)
println("__VERIFY_RESULT__ " + id + " " + status + " " + count_str + " vs " + target)
if status == "FAIL" { exit(1) }
