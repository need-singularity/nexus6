// AUTO-GENERATED-EXEMPLAR — calc/anima_verify/manifest.jsonl line 5
// generator: calc/anima_verify/generator.hexa (template: saturation_count)
// gate: G_VALIDITY  hyp: M1-M10 discovery method enumeration MECE saturated
//
// circular-trap-free contract:
//   target  = 10                                   ← anima/.roadmap.philosophy
//   formula = count(@L ANIMA-discovery-M*)         ← atlas.append shard
//   inputs  = atlas.append.anima-discovery-verification-methods file ← atlas anchor
//
// LLM judge 금지 — deterministic grep count.

let id = "anima_meta_M_discovery_count_saturated"
let target = "10"
let target_n: i64 = 10

let home = env("HOME")
let shard = home + "/core/canon/atlas/atlas.append.anima-discovery-verification-methods-2026-05-07.n6"
let cmd = "grep -c '^@L ANIMA-discovery-M' " + shard + " 2>/dev/null || echo 0"
let out = exec(cmd).trim()
let actual = to_int(out)
let status = if actual == target_n { "PASS" } else { "FAIL" }
println("verify: " + id)
println("  shard:  " + shard)
println("  count:  " + to_string(actual))
println("  target: " + target + " (M1-M10 MECE saturation)")
println("  status: " + status)
println("__VERIFY_RESULT__ " + id + " " + status + " " + to_string(actual) + " vs " + target)
if status == "FAIL" { exit(1) }
