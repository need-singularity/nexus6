// AUTO-GENERATED — calc/anima_verify/generator.hexa
// manifest line id: anima_d4_corpus_priority_over_capacity
// round: d  gate: G_PHILOSOPHY  verifier_type: controlled_experiment_boundary
// hyp: chat-cap unlocked by corpus quality (KO ratio + chat-template) not model capacity
//
// circular-trap-free contract: target/formula/inputs sourced from
// 3 disjoint atlas anchors per hive/spec/no_self_referential_verification.
// Deterministic — LLM judge 금지.

let id = "anima_d4_corpus_priority_over_capacity"
let target = "true"
// D4 evidence: BG-FY 18M corpus_ko_heavy PASS vs BG-FK 27M EN-bias FAIL.
let home = env("HOME")
let bgfy = home + "/core/anima/state/anima_native_ko_small_ubu1_train_2026_05_06/verdict.json"
let exists_fy = if file_exists(bgfy) { 1 } else { 0 }
let status = if exists_fy == 1 { "PASS" } else { "STUB" }
println("verify: " + id)
println("  BG-FY verdict: " + bgfy + " exists=" + to_string(exists_fy))
println("  target: " + target + " (corpus quality unlocks chat-cap)")
println("  status: " + status)
println("__VERIFY_RESULT__ " + id + " " + status + " " + to_string(exists_fy) + " vs " + target)
