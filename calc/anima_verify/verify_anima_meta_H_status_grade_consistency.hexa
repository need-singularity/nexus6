// AUTO-GENERATED — calc/anima_verify/generator.hexa
// manifest line id: anima_meta_H_status_grade_consistency
// round: meta  gate: G_VALIDITY  verifier_type: status_grade_mapping
// hyp: H_NNN frontmatter status maps to atlas grade per documented rule
//
// circular-trap-free contract: target/formula/inputs sourced from
// 3 disjoint atlas anchors per hive/spec/no_self_referential_verification.
// Deterministic — LLM judge 금지.

let id = "anima_meta_H_status_grade_consistency"
let target = "consistent"
// Cross-check H_NNN frontmatter status field maps to atlas @? grade.
// STUB: full check requires YAML frontmatter parser + atlas regex; emits SKIP.
println("verify: " + id)
println("  target: " + target + " (32 H_NNN status->grade consistent)")
println("  status: STUB (frontmatter YAML parser pending)")
println("__VERIFY_RESULT__ " + id + " STUB 0 vs " + target)
