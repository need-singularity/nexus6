name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'n6/atlas.n6'
      - '.github/workflows/pages.yml'
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Sync atlas.n6 into docs
        run: |
          cp n6/atlas.n6 docs/atlas.n6
          ls -la docs/atlas.n6
      - name: Resolve symlinks in docs (prevent tar race on growth_bus.jsonl)
        run: |
          # Replace any symlinks in docs/ with their current target contents so
          # upload-pages-artifact's tar step doesn't race against a mutating
          # symlink target (observed failure: "File removed before we read it").
          find docs -type l | while read link; do
            target=$(readlink -f "$link" || true)
            rm -f "$link"
            if [ -n "$target" ] && [ -e "$target" ]; then
              cp -a "$target" "$link" || true
            else
              touch "$link"
            fi
          done
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
