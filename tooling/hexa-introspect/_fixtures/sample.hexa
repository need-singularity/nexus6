// sample fixture
/// Greet the user.
fn greet(name: string) -> string {
    return "hello " + name
}

let TWO = 2
const PI = 3.14
let mut counter: i64 = 0

pub fn add(a: i64, b: i64) -> i64 {
    return a + b
}

struct Point {
    x: i64,
    y: i64
}

enum Color { Red, Green, Blue }
