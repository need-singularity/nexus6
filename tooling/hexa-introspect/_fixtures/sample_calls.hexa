// fixture with calls — for --diff-rename
/// Greet the user.
fn greet(name: string) -> string {
    return "hello " + name
}

fn main_routine() -> i64 {
    let m1 = greet("alice")
    let m2 = greet("bob")
    println(m1)
    println(m2)
    let m3 = greet("carol")
    println(m3)
    return 0
}
