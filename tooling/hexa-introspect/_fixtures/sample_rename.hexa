// sample fixture (rename: greet -> hail, _ns_now -> _clock_ns added)
/// Greet the user.
fn hail(name: string) -> string {
    return "hello " + name
}

let TWO = 2
const PI = 3.14
let mut counter: i64 = 0

pub fn add(a: i64, b: i64) -> i64 {
    return a + b
}

struct Point {
    x: i64,
    y: i64
}

enum Color { Red, Green, Blue }

fn _clock_ns() -> i64 {
    return 1234567890
}
