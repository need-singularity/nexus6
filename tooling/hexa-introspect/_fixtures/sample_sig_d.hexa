// sig fixture D — qux: param renamed (input -> text), quux: + optional param with default
fn qux(text: string) -> string {
    return text
}

fn quux(a: i64, b: i64 = 42) -> i64 {
    return a + b
}
