// sig fixture B — added required param to foo, removed param from bar, return type change of baz
fn foo(a: i32, b: i64) -> i32 {
    return a + 1
}

fn bar(x: i64) -> i64 {
    return x
}

fn baz(name: string) -> i64 {
    return 0
}
