; tree-sitter-hexa — tags.scm
; Symbol index for ctags-style navigation (e.g. nvim-treesitter, gh code search).

(function_declaration
  name: (identifier) @name) @definition.function

(let_declaration
  name: (identifier) @name) @definition.constant
