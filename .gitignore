/target
shared/tecsrs/target
tools/*/target
.claude/*
!.claude/settings.json
!.claude/hooks/
shared/__pycache__
__pycache__
*.pyc
*.whl
*.so
*.dylib

# large data files (local-only, regenerable)
shared/discovery_log.jsonl
shared/discovery_log.jsonl.*

# singularity recursion runtime state (large, regenerable)
shared/discovery/cycle/edges.jsonl
shared/discovery/cycle/topology.jsonl
shared/discovery/cycle/wal.jsonl
shared/discovery/cycle/state.lock
shared/discovery/cycle/*.html
shared/.bt_cooldown
shared/.hexa_lang_cooldown
shared/.improver_cooldown_*
shared/discovery/rng_lab/.anu_cache.bin
shared/auto_bt.log
shared/cmd_router.log
shared/growth_tick_preflight.log
shared/auto_gap.log
shared/.autolink_out.log*
shared/discovery_archive_*.jsonl
shared/growth_bus_archive_*.jsonl

# new categorized paths (post-migration e359047)
shared/discovery/discovery_log.jsonl
shared/discovery/discovery_log.jsonl.*
shared/discovery/archive/
shared/discovery/growth/growth_bus_archive_*.jsonl

# auto-generated verification stubs
shared/calc/auto_stubs/

# large binary datasets (MNIST etc.) — downloadable, not for git
shared/calc/data/

# growth engine lock
shared/.growth_engine.lock

# backup artifacts
*.bak-*

# playwright MCP runtime
.playwright-mcp/

# temporary screenshots
pages-404.png
shared/__pycache__/

# atlas.n6 generated sidecars + backups + locks
shared/n6/atlas.n6.stats
shared/n6/atlas.n6.deg
shared/n6/atlas.n6.bak.*
shared/n6/*.bak
shared/n6/atlas.n6.backup_*
shared/n6/atlas.n6.lock
shared/n6/atlas.n6.bt_shell.lock
shared/n6/atlas.n6.pre_shard_*
shared/n6/atlas.n6.hot
shared/n6/atlas.n6.cold
shared/n6/atlas.n6.shard_meta.json

# closed_loop_verify --apply backups (atomic backup before jq patch)
shared/consciousness/consciousness_laws.json.bak.*
shared/consciousness/consciousness_laws.json.tmp_*

# runtime dispatch state (host-specific, regenerated by load_balancer.hexa)
shared/dispatch_state.json

# phase45 symmetry sweep output (regenerable, ~280KB)
shared/n6/atlas_phase45_symmetry.jsonl

# Phase 6 cross-scale proposal output (regenerable via shared/n6/scripts/atlas_phase6_crossscale.py)
shared/n6/atlas_phase6_crossscale.jsonl

# Phase 46 canonical-constant node proposal (regenerable via shared/n6/scripts/atlas_phase46_canonical_nodes.py)
shared/n6/atlas_phase46_canonical_nodes.jsonl

# Phase 47 canonical cross-domain @S bridges (regenerable via /usr/bin/python3 ... atlas_phase47_canonical_bridges.py)
shared/n6/atlas_phase47_canonical_bridges.jsonl

# Phase 48 approx-expression cross-domain @S bridges (regenerable via /usr/bin/python3 ... atlas_phase48_approx_expr_bridges.py)
shared/n6/atlas_phase48_approx_expr_bridges.jsonl

# runtime-generated directories and files (engine-recreated)
shared/n6_mirror/
shared/growth_bus.jsonl

# runtime logs directory
shared/logs/

# large runtime data files (regenerable, not for git)
shared/discovery/cycle/topology_mk2.jsonl
shared/bt/auto_bt.log
shared/hexa/hexa_pitfalls_log.jsonl
shared/discovery/verified_constants.jsonl
shared/discovery/discovery_graph.json
shared/discovery/module_candidates.jsonl
shared/discovery/growth_bus.jsonl

# DSE large results (regenerable via dse cross-domain sweep)
shared/dse/dse_cross_results.json

# runtime bridge state (regenerated by engine)
shared/bridge_state.json

# hexa-lang mirror (moved to shared/hexa/)
shared/hexa-lang/

# archive artifacts (compressed, regenerable)
shared/archive/*
/archive/
# exception: tracked refactor snapshots
!shared/archive/hooks-20260414/

# DSE regenerable cross-domain data
shared/dse/cross/pair_scores.jsonl

# bt backup artifacts
shared/bt/*.before_unicode

# blowup runtime outputs (regenerable)
cli/blowup/*_report.json
cli/blowup/**/*_report.json
cli/blowup/*.log

# one-off screenshot PNGs (root)
*.png

# docs atlas mirror (SSOT is shared/n6/atlas.n6)
docs/_mirror_atlas.*

# lens porting logs
shared/lenses/.chunk_*
shared/lenses/.port_*

# absorbed growth metadata (completed, not needed in git)
.growth/
GROWTH.md
.shared

# regenerable data
data/

# mk2_hexa — atlas KB recall layer (2026-05-06: 6 atlas modules + main dispatcher + tests)
# Previously gitignored as "scaffold (hello world, unused)" — now active SSOT.

# dead launchd plists (all unloaded)
launchd/
# exception: nexus 자체 launchd plist (cl-refresh 30m 주기)
!shared/launchd/
!shared/launchd/*.plist
# exception: ω-bridge-20 active hourly bridge pipeline plist
!config/launchd/
!config/launchd/*.plist

# config loop logs
shared/config/loop/logs/

# legacy bt patches (already applied)
shared/bt/*.diff

# regenerable n6 artifacts
shared/n6/math_atlas.db
shared/n6/math_atlas.dot
shared/n6/atlas_tree.txt

# regenerable calc output
shared/calc/auto_grade_results.csv

# systemd (Mac — unused)
systemd/

# OS metadata
.DS_Store
shared/discovery/cycle/edges.adjacency.json
shared/discovery/cycle/edges.adjacency.json.gz
shared/discovery/cycle/edges.adjacency.shard_[0-9]*

# Claude Code memory (account-shared, local only)
.claude-memory/

# runtime state (probe/loop artifacts — regenerated on demand)
shared/infra_state.json
shared/task_queue.jsonl

# cl 계정관리 런타임 (R14 SSOT, local-only, regenerable)
shared/.runtime/

# harness runtime state (H-COMMIT/H-SESSION-* session-local, regenerable)
shared/harness/.session_id_*
shared/harness/.cli_budget_state.json
shared/harness/.git_fresh_state.json
shared/harness/broadcast.jsonl
shared/harness/session_registry.jsonl
shared/harness/work_registry.jsonl
shared/harness/commit_log.jsonl
shared/harness/gauge_state.json
shared/harness/mcp_health.json
shared/harness/observations.jsonl
shared/harness/sandbox_breaches.jsonl

# 2026-04-22 autonomous session additions — post-shared/ migration
# claude-code hook runtime (self-seeded stack, local-only)
.hook-commands/
.runtime/
# repo-root infra/bridge/dispatch state (runtime)
/bridge_state.json
/dispatch_state.json
/infra_state.json
# growth bus (generated every tick)
/growth_bus.jsonl
/discovery_log.jsonl
/task_queue.jsonl
discovery/growth_bus.jsonl
discovery/discovery_graph.json
discovery/rng_lab/.anu_cache.bin
# convergence/discovery runtime logs
convergence/_*.jsonl
discovery/cycle/wal.jsonl
discovery/cycle/topology.html
discovery/cycle/topology_mk2.jsonl
discovery/cycle/edges.adjacency.*
# backup markers from this session
*.bak
*.bak.2026-*
*.bak.pre-*
*.before_unicode
CLAUDE.md.bak
# per-session tool state
bt/auto_bt.log*
tool/observations.jsonl
.goal_growth_state.json
# n6 derivatives (generated from atlas)
n6/atlas.*.deg
n6/atlas.*.stats
n6/atlas.*.shard_meta.json
n6/atlas.blowup.jsonl
n6/atlas.blowup.events.jsonl
n6/atlas_phase4*_*.jsonl
n6/atlas_bloom.hexa
n6/atlas_hot_shard.hexa
# config loop logs
config/loop/logs/
build/

# 2026-04-22 — runtime append-only logs (per safe-commit protocol)
tool/lint_log.jsonl
tool/mistakes.jsonl

# 2026-04-22 wave 2 — post-shared/ migration residuals (restored after PR #40 inadvertently dropped them)
# runtime logs dir (entire tree: stderr/stdout launchd logs, corpus pipes, rotated gzips)
logs/
# experimental state snapshots (regenerable; not source-of-truth)
# NB: use `state/*` (not `state/`) so explicit un-ignores below can re-admit
# specific subtrees. Git cannot un-ignore contents of a directory excluded
# wholesale via a trailing-slash rule.
state/*
# G3 proposal stack — inventory.json + README + 모든 proposal *.json 은 SSOT
!state/proposals/
!state/proposals/inventory.json
!state/proposals/README.md
!state/proposals/*/
!state/proposals/*/.gitkeep
!state/proposals/*/*.json
# ops runbooks (console handoff, recovery procedures) — human-authored SSOT
!state/ops/
!state/ops/*.md
# kick(ω-cycle) registry — own:atlas-absorb-mandatory triad SSOT (strata/axes/backends/noise_sources)
!state/kick/
!state/kick/registry/
!state/kick/registry/*/
!state/kick/registry/*/*.hexa
!state/kick/registry/*/*/
!state/kick/registry/*/*/*.hexa
!state/kick/registry/*/*/.gitkeep
state/cross_repo_sync_log.jsonl
state/cross_repo_links.jsonl
# auto-generated verification stubs (root)
calc/auto_stubs/
# discovery runtime logs and big datasets (root path mirror)
discovery/*.jsonl
discovery/*.txt
# bench generated seeds / test-prompt streams
bench/hexad_closure_seeds/
bench/*_test_prompts.jsonl
bench/btr_evo5_holographic_ib.jsonl
# DSE cross-domain sweep outputs (root path mirror)
dse/cross/
dse/dse_cross_results.json
# n6 runtime derivatives (root path mirror)
n6/math_atlas.db
n6/math_atlas.dot
n6/atlas_phase6_crossscale.jsonl
n6/revive_canary.events.jsonl
n6/revive_progress.events.jsonl
# n6_mirror (engine-rebuilt)
n6_mirror/
# hexa pitfalls log (append-only, large)
hexa/hexa_pitfalls_log.jsonl
# tool transient cache / current lint scratch
tool/depth_bracket_cache.json
tool/lint_current.txt
# scripts sync-state cache
scripts/.sync_state.json
# void marker (single-file probe token)
/void
# lb runtime state (airgenome bin/lb.sh atomic-writes per probe — churn, not source)
/lb_state.json

# 2026-04-27 runtime caches + session state (safe-commit protocol)
harness/.git_fresh_state.json
tool/.cli_budget_state.json
tool/loop_state.jsonl
tool/gap_monitor.log
tool/roadmap_sync.jsonl
state/kick/parent_sid.txt

# 2026-05-05 — tool/ runtime artifacts (per-session markers, ledger snapshots,
# transient configs). ~3200 .session_id_* files were tracked accidentally
# (one per harness session). Plus a handful of mixed runtime configs.
# Code (.hexa/.sh/.py/.plist/.md/.yaml) and audit-log .jsonl files remain tracked.
tool/.session_id_*
tool/.consensus_state.json
tool/.consensus_seen.tsv
tool/.discovery_infra_state.json
tool/.gc_weekly_cooldown
tool/.git_fresh_state.json
tool/.reaper_counter
tool/mcp_health.json
tool/sample.md
tool/severity_map.json

# 2026-05-05 cleanup — kick(ω-cycle) per-run dispatch intermediate state.
# Each state/kick/runs/<slug>/ dir holds runtime artifacts (prompt.txt,
# noise_envelopes.jsonl, child_out.log) used during subagent dispatch.
# Permanent witnesses are emitted to design/kick/*.json (tracked SSOT).
# NB: ~135 historical run dirs (2026-04-26 to 2026-04-28) remain tracked
# as accidental commits; future runs are correctly excluded by this rule.
state/kick/runs/
