@track R4_SUBAGENT_DISPATCH_SUB_AB
@state completed
@date 2026-04-27
@chain ←r4_blacklist_retire_simplification_2026_04_27.convergence (Sub-AA) ←r4_kick_sse_drain_timeout_2026_04_27.convergence (Sub-Y)
@scope sub-AB — hive `tool/subagent_dispatch.hexa` triple-fix delivering raw 44 invariants: free-port-pick + state-dir self-heal + stale-port reclaim. Closes the kick-infra-broken evidence cluster surfaced this session (port 19877 hardcode collision, ~/.hive/subagent-state inode anomaly, 22-min hang).
@principle 근본해결 + 자기치유 + 운영 cost cap

# 진단 (raw 100 falsifier candidate P2026-04-27-305 evidence)
@evidence_a `lsof -i :19877` showed PID 38584 (active hive node) LISTENing + 19877-19880 4 stale node procs blocking new spawn. raw 44 explicitly mandates free-port-pick in [19877, 20000].
@evidence_b `ls -la ~/.hive/subagent-state` → regular file (0 bytes) instead of directory; tool/subagent_dispatch.hexa _state_path treats parent as dir → write fails silently.
@evidence_c noise-envelope ω-cycle (Sub-Y) 22-min hang from agent.end suppression by hive-telegram extension stale-session warning. Sub-Y idle-timeout watchdog already capped this; Sub-AB adds the orphan-process reclaim primitive.

# 시행 (Agent dispatch — raw 104 means-end decoupling)
@dispatcher_choice nexus kick infra broken → Agent tool subagent (raw 100 fail-back per P2026-04-27-305).
@scope_targets `/Users/ghost/core/hive/tool/subagent_dispatch.hexa` (uchg unlock-edit-relock).
@diff_summary +430/-19 lines (823→1225).
@fix_1_free_port_pick lines 290-333: `_port_in_use(port)` (lsof primary, nc fallback) + `_pick_free_port()` scan [PORT_LO=19877, PORT_HI=20000]. Exhaustion site (line 1129-1138): raw 66 trailer `reason=port-range-exhausted fix=reclaim --dry-run / reclaim / lsof confirm / widen`.
@fix_2_state_dir_self_heal lines 187-282: `_state_dir_self_heal_path()` probes inode shape, on file/symlink/socket → `mv → .bak.<ts>` + `mkdir -p` + copy non-empty legacy contents. Trigger at top of `main()` (line 1129) before `_pick_free_port`. **Live verification**: `~/.hive/subagent-state` 0-byte file auto-healed during agent's smoke run; bak preserved at `~/.hive/subagent-state.bak.1777297052`.
@fix_3_stale_port_reclaim lines 730-866 + 1036-1064: new CLI `subagent_dispatch.hexa --reclaim-stale-ports [--dry-run] [--min-age-sec <n>]` (default 600s/10min). `_active_ports_csv()` reads state-root `status running` blocks; `_find_subagent_children()` ps-grep canonical `--serve --port N --no-session`; `_reclaim_stale_ports()` TERM 1s KILL. raw 80 sentinel `__SUBAGENT_RECLAIM__ <PASS|FAIL> killed=<N> kept=<M> dry_run=<bool> min_age_sec=<n>`. Per-process raw 66 verdicts (keep/keep-young/would-kill/killed/kill-failed).

# 검증
@selftest_pass `hexa run tool/subagent_dispatch.hexa --selftest` → `__SUBAGENT_DISPATCH_SELFTEST__ PASS` (5 fixture assertions).
@fixture_port python listener bound on 29977 → `_port_in_use` true; teardown → false. PASS.
@fixture_state /tmp/hive-selftest-state-fixture-<ts> file → renamed `.bak.<ts>` + dir created. PASS.
@fixture_reclaim fake stale child via `exec -a 'node-fake-subagent ... --port 29981 ...' sleep 30` → finder locates → dry-run leaves alive → real run TERM+KILL confirmed. PASS.
@live_self_heal ~/.hive/subagent-state was actually 0-byte file when agent fired; auto-heal mv'd to .bak.1777297052 + recreated as directory. Real-world smoke validation, not synthetic.

# Commit + audit
@commit hive bd3dd9319 (local, branch main, NOT pushed — caller responsibility).
@audit_row nexus/.raw-audit ts=2026-04-27T22:40:00Z actor=sub-AB op=unlock-edit-relock authority="raw 1 + raw 44 + raw 66 + raw 80 + raw 85" verified="live PASS + auto-heal evidence".

# follow-up
@followup_p305_apply raw 100 strengthening proposal P2026-04-27-305 (status pending) can now cite Sub-AB land — raw 44 invariants restored, kick-infra-broken falsifier evidence chain partially closed. Apply path 100-falsifier에 `port-leak / state-corruption / SSE-hang` 3-evidence enumerate + Sub-AB-fix pointer.
@followup_cron_reclaim `--reclaim-stale-ports` 정기 호출 (raw 99 launchd-standalone ban → hive init body 통합 권장).
@followup_p305_resolve_via_sub_ab P305 status pending → "Sub-AB lands kick-infra self-heal" 으로 strengthen 적용 가능.

@verdict Sub-AB landed in hive (commit bd3dd9319). raw 44 invariants restored: dispatcher survives concurrent hive sessions (free-port-pick) + recovers from state corruption (self-heal) + bounds orphan port leak (reclaim). 22-min hang surfaced via Sub-Y idle-timeout, infrastructure root cause closed via Sub-AB.
