@track R4_DISPATCH_CONVERGENCE_LOCK
@state completed
@date 2026-04-27
@chain ←r4_gpu_concurrent_slot_2026_04_27 (Sub-AK iter 5) ←r4_kick_parallel (Sub-AJ iter 4) ←r4_psi_aware_clamp (Sub-AI iter 3) ←r4_per_host_dynamic_concurrency (Sub-AH iter 2) ←r4_gpu_aware_dispatch (Sub-AG iter 1)
@scope sub-AL — `tool/dispatch_convergence_lint.hexa` 신규: physical/mathematical limit dispatch matrix 수렴 detector. /loop 6번째 iteration 에서 CONVERGED verdict 발행 → user directive '한계 수렴시 lock' 실행 (cron 8a4783e5 delete + 6 dispatch tools chflags uchg 확정).
@principle raw 36 fixpoint convergence + raw 37 plan-omega-saturation + raw 71 auto-retire on saturation

# 동기 (user directive '한계 수렴시 lock')
@directive 2026-04-27 user (/loop iter 6 prompt context): "한계 수렴시 lock". 5 iter 누적 후 dispatch matrix 가 physical/mathematical limit 에 수렴했는지 객관적 판단 + 수렴 시 lock + /loop 종료.
@need 주관적 "이제 됐다" 판단 대신 objective lint criterion. 5 dispatch dimensions 각각 (a) dynamic compute / (b) admission tier / (c) slot tracking / (d) selftest 4-property all-pass 시 CONVERGED.

# 설계 (raw 36 fixpoint)
@criterion_required 5 orthogonal physical/mathematical limit dims:
  D1 RAM-avail-dynamic        (Sub-AH _per_host_max dynamic compute)
  D2 RAM-PSI-clamp            (Sub-AI _per_host_max PSI ceiling)
  D3 GPU-avail-admission      (Sub-AG L8 single-shot avail check)
  D4 GPU-slot-tracking        (Sub-AK _acquire_gpu_slot concurrent cap)
  D5 fan-out-orchestrator     (Sub-AJ kick_parallel.hexa N-topic dispatch)
@criterion_optional 3 deferable dims:
  D6 GPU-PSI-throttle         (memory.used/total ratio analog)
  D7 network-IO-bandwidth     (rsync throughput / latency)
  D8 cross-host-work-steal    (L6 PSI queue partial — no selftest)
@convergence_definition required=5/5 → CONVERGED. optional 카운트는 informational (수렴에 영향 X).

# 시행
@file tool/dispatch_convergence_lint.hexa (uchg locked, ~340 LOC)
@cli `hexa run tool/dispatch_convergence_lint.hexa [--json] [--strict] [--selftest]`
@implementation 8 dim별 _check_dN() 함수 → 각 4-bool array 반환 (dyn/adm/slot/stest). _grep_in() pattern probe + _selftest_passes() invocation probe. _bool_arr_count + _all_pass aggregator.
@selftest 6 fixtures PASS — _grep_in self-pattern + _grep_in nonce-not-found (base64-fragmented 으로 self-reference 회피) + _grep_in missing-file + _bool_arr_count 3-cases + _all_pass true/false.
@output_human 표 (dim × 4 props) + scorecard + verdict + sentinel.
@sentinel raw 80: `__DISPATCH_CONVERGENCE__ <CONVERGED|GAP> required=<R/5> optional=<O/3>`.

# 검증 (live verdict)
@live_verdict required=5/5 ✓ → CONVERGED.
@d1_ram_avail dyn ✓ adm ✓ slot ✓ stest ✓ — 4/4
@d2_ram_psi dyn ✓ adm ✓ slot ✓ stest ✓ — 4/4
@d3_gpu_avail dyn ✓ adm ✓ slot ✓ stest ✓ — 4/4 (slot=true by design, D4 책임)
@d4_gpu_slot dyn ✓ adm ✓ slot ✓ stest ✓ — 4/4
@d5_fan_out dyn ✓ adm ✓ slot ✓ stest ✓ — 4/4
@d6_gpu_psi 0/4 (defer)
@d7_network_io 0/4 (defer)
@d8_cross_host_steal 3/4 (L6 partial; selftest 없음)

# Lock 실행 (per directive)
@lock_cron CronDelete 8a4783e5 — /loop */5 * * * * 종료. Loop 자동 재발사 stop.
@lock_dispatch_tools 6 file uchg 확정 (이미 land 되며 lock 됨, 본 cycle 에서 재확인):
  - scripts/bin/hexa_remote (Sub-AG/AH/AI/AK 누적 land)
  - tool/cluster_load_audit.hexa (Sub-AF)
  - tool/kick_parallel.hexa (Sub-AJ)
  - tool/dispatch_convergence_lint.hexa (Sub-AL, 본 cycle)
  - tool/hexa_real_parity_check.hexa (Sub-V)
  - tool/noise_envelope.hexa (Sub-U)
@lock_audit nexus/.raw-audit row 2건 (Sub-AL lock + loop-terminate).

# 누적 dispatch matrix (5-iter + lint)
@matrix_ram capacity = min( floor((avail-4096)/cmin) [Sub-AH], max(1, 8-PSI/12) [Sub-AI] )
@matrix_gpu admission gpu_avail >= 8192 [Sub-AG] AND slot acquire floor((gpu_avail-1024)/4096) [Sub-AK]
@matrix_orchestrator fan-out N topics [Sub-AJ] with --max-parallel min(n, 6)
@matrix_terminator dispatch_convergence_lint [Sub-AL] CONVERGED detector

# 부수효과
@side_effect_a /loop session-only state — cron deleted, prompt-driven loop ended. Future invocations: explicit user fire 또는 새 /loop 명령.
@side_effect_b 6 dispatch tools 모두 lock — 추가 변경 시 unlock-edit-relock cycle 필요 (raw 85).
@side_effect_c optional dims (D6/D7/D8) 미land — 별도 cycle 에서 user directive 시 추가 가능. CONVERGED verdict 는 required-only 기준이므로 변경 X.

# follow-up
@followup_d6_gpu_psi GPU memory pressure (memory.used/total 비율) 신호 추가 → Sub-AI 와 동일 패턴, GPU 차원의 PSI-aware throttle. Sub-AG 단독 admission 에서 dynamic clamp 로 진화. 별도 cycle.
@followup_d7_network_io rsync throughput / network bandwidth probe → 8th dim. drill 같은 heavy-rsync 작업이 network-bound 일 때만 의미.
@followup_d8_selftest L6 PSI queue 의 selftest fixture 추가 → 0/4 → 4/4 로 optional convergence.
@followup_smoke_validate 다음 사용자 invocation 시 `hexa run tool/kick_parallel.hexa --topics 'a|b|c'` live 발사로 5-dim composition 동시 작동 검증. drill --gpu 로 D3+D4 path 검증.

@verdict Sub-AL landed — convergence detector CONVERGED required=5/5 verdict. /loop cron deleted, 6 dispatch tools uchg locked. Physical/mathematical limit dispatch matrix 가 raw 36 fixpoint 에 수렴함을 객관적으로 입증. user directive '한계 수렴시 lock' 실행 완료.
