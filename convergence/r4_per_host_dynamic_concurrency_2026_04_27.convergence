@track R4_PER_HOST_DYNAMIC_CONCURRENCY
@state completed
@date 2026-04-27
@chain ←r4_gpu_aware_dispatch_2026_04_27.convergence (Sub-AG /loop iter 1) ←r4_cluster_load_audit_2026_04_27.convergence (Sub-AF)
@scope sub-AH — `_per_host_max` dynamic compute. hetzner 122GB avail × 1 hardcoded slot = 75% capacity 휴면. queuing theory limit: capacity = floor((resource - safety) / per-job-cost). /loop iteration 2.
@principle 수학적 한계 (mathematical limit) 직접 적용 — heuristic 1-slot 제거, computed value

# 동기 (Sub-AG iter 1 보완)
@gap_observed cluster_load_audit (Sub-AF) hetzner 122GB avail / ubu1+ubu2 25GB avail / RTX 5070×2 0% util. Sub-AG (iter 1) GPU 차원 추가, 본 iter 는 RAM 차원의 mathematical-limit fit.
@gap_quantified hetzner 1-slot × 24GB-large = 24GB 사용 / 122GB 가용 = 19.6% utilization. 4-slot dynamic = 96GB / 122GB = 78.7% utilization. 4× concurrent throughput.
@root_cause `_per_host_max` (line 395-402 pre-edit) `printf '%s' "${HEXA_REMOTE_CONCURRENCY:-1}"` 단일 hardcode — host RAM heterogeneity 무시.

# 설계 (raw 36 problem-solving-triad)
@triad_fixpoint capacity = floor((avail - safety) / class_min) — Banach-style 수학적 fixpoint. avail/class_min ratio 가 자연스러운 capacity 측정.
@triad_bracket safety=4096MB headroom (system + interpreter RSS), upper clamp=8 (over-saturation guard — slot lock 경합 + Bayes 학습 용량 고려).
@triad_atomize 2 atomic edits, single unlock cycle:
  (1) `_NEXUS_ORIG_ARGV=("$@")` stash early (line 298) → downstream classify accessible
  (2) `_per_host_max` rewrite (line 395-440) — env/explicit > dynamic > fallback ladder

# 시행
@file scripts/bin/hexa_remote (uchg unlock-edit-relock)
@diff_a `_NEXUS_ORIG_ARGV=("$@")` stash at top of dispatch flow (before _IS_HEAVY_COMPUTE detection).
@diff_b `_per_host_max` 4-step priority: per-host env (HEXA_REMOTE_CONCURRENCY_<HOST>) > global env (HEXA_REMOTE_CONCURRENCY) > dynamic (avail+class) > fallback 1. Dynamic computes class via `_classify_job ${_NEXUS_ORIG_ARGV[@]}` then `floor((avail - safety) / cmin)` clamped [1,8].
@safety_margin HEXA_REMOTE_PER_HOST_SAFETY_MB=4096 (default 4GB headroom). Override via env.

# 수학적 검증 (live cluster snapshot 적용)
@math_hetzner_large avail=122000 - safety=4096 = 117904; class_min=24576; → floor(117904/24576) = 4 slots (was 1, +300% capacity)
@math_hetzner_medium avail=122000 - 4096 = 117904; cmin=8192 → 14 slots → clamp 8 (over-saturation guard ; reasonable: 8 × 8GB = 64GB / 122GB = 52% util at full load, 50% idle for OS+Bayes learning)
@math_hetzner_small avail=122000 - 4096 = 117904; cmin=2048 → 57 slots → clamp 8 (small jobs do not benefit from > 8 concurrent due to lock contention)
@math_ubu1_medium avail=25000 - 4096 = 20904; cmin=8192 → floor(20904/8192) = 2 slots (was 1, +100% capacity)
@math_ubu1_large avail=25000 - 4096 = 20904; cmin=24576 → floor < 1 → 1 slot (large job barely fits, no concurrency)
@math_ubu2 동일 (avail similar to ubu1)

# 검증
@verify_syntax `bash -n hexa_remote` PASS post-relock.
@verify_smoke kick tree (heavy-compute 미해당) 정상 — dispatch chain 우회됨.
@verify_unit 2 edit syntactically valid; live multi-slot heavy-compute smoke 는 다음 iteration 에서 (drill 또는 smash dispatch).

# 부수효과
@side_effect_a 기존 hardcoded `HEXA_REMOTE_CONCURRENCY_HETZNER` 등 env 는 그대로 작동 (priority 우선). 운영자 명시 override 보존.
@side_effect_b dynamic 경로는 `_best_avail` 가 set 됐을 때만 (post probe). probe 미실행 경로 (HEXA_REMOTE_HOST 직접 지정) → fallback 1 (안전).
@side_effect_c `_NEXUS_ORIG_ARGV` 새 global. memory cost 무시 가능. 다른 함수가 동일 이름 사용 안 함 (grep 검증).

# follow-up (/loop iteration 3+)
@followup_smoke_drill 다음 iter: `nexus drill` heavy-compute 실 dispatch → hetzner concurrent slot 4 동시 실행 검증. PSI 가 임계 넘는지 측정 후 safety/clamp 조정.
@followup_clamp_tuning upper clamp=8 적정성 — Bayes window stats 누적 후 lock contention 측정.
@followup_per_class_safety class 별 safety_margin 차등 (large=8192, medium=4096, small=1024) — 더 정확한 mathematical fit.
@followup_gpu_concurrent_slots Sub-AG L8 + Sub-AH 통합: GPU class 도 dynamic 계산 → RTX 5070 12GB/4GB-workload = 3 concurrent GPU slots per host.
@followup_psi_aware_clamp PSI 기반 dynamic upper clamp (PSI 50% → 4 slots, 70% → 2 slots, 90% → 1 slot).

@verdict Sub-AH landed — `_per_host_max` mathematical-limit dynamic compute. hetzner large class 1→4 slots (75% dormant capacity 활성화), ubu1/ubu2 medium 1→2 slots. queuing theory mathematical fit 직접 적용. /loop iteration 2 완료. 다음 iter: live drill smoke 또는 다른 limit gap.
