# Anima → hexa-lang / sister Handoff (2026-04-19)

nexus bus 경유. 2026-04-19 wakeup 세션에서 발견/정리된 항목 append. 기존 2026-04-18 handoff 연속선상.

## 2026-04-19 Wakeup Addendum

2026-04-19 wakeup 세션에서 발견된 신규 에스컬레이션 + 분석 결과. P5 우선순위 sister 측 요청 1건 + feasibility/정리 항목 4건.

### P5 신규 에스컬레이션: hxcuda_softmax forward C-linkage export

- **배경**: anima 측 GPU dispatch 경로 2 (`train_clm.hexa` `HXBLAS_BACKEND=cuda` 분기) 를 시작하려면 `hxcuda_softmax` 순방향을 C-linkage symbol 로 export 필요
- **현재 상태**: `nm -D /usr/local/lib/libhxcuda.so` 결과 C++ mangled 심볼만 존재
  - `_Z21hxcuda_softmax_kernelPKfPfS1_ii` + `__device_stub__` 계열
  - C-linkage wrapper 없음 → anima 측 FFI 바인딩 불가
- **요청**: sister 측에 아래 export 추가
  ```cpp
  extern "C" void hxcuda_softmax(const float* logits, float* probs, float* lse, int n, int K)
  ```
- **Anima 영향**: `train_clm` L1256 `hxblas_attn_softmax_causal` 대체 경로 enabler — GPU dispatch 경로 2 의존성

### GPU 경로 2 feasibility 공유

- **작업량**: 4–6h, 중간 위험도
- **리스크 요인**:
  - bf16 ↔ fp32 casting boundary 정의 필요
  - 부재 심볼 3건: `hxcuda_softmax` forward (C-linkage), + 관련 backward/reduce 류
- **대안 경로**:
  - **Path 1**: sister `hxblas_cuda.cu` shim 신규 작성 (6–8h)
  - **Path 3**: runpod CPU fallback ($3/hr, 즉시 가용)

### R37 round 3 발견 (Python 금지 감사)

- **training/deploy/__pycache__**: 2 stale `.pyc` 잔존 (R37 이전 잔재, `rm` 권고 pending)
- **ready/ submodule**: 6 개 `__pycache__` + 300+ 활성 `.py` → R37 위반 상태
  - submodule scope → 별도 마이그레이션 세션 필요 (본 세션 범위 외)
- **.gitignore coverage**: OK — leak 없음 확인

### CVF 이중 파일 상태 (verification)

- `anima-core/verification/cvf.hexa` — 12 LOC R37 stub
- `ready/core/verification/cvf.hexa` — 694 LOC full impl, 4-Layer zombie control 포함
- **호출 경로**: `launch.hexa:220` 이 **stub** 측 호출 중
  - `verify` 명령은 opt-in 이므로 부팅 비치명 (non-blocking)
- **권고**: 장기적으로 `ready/` impl → `anima-core` pure-hexa port (Option B)

### BG #64 상태 (정보 공유)

- PID **2017609**, hexa build 26m+ 컴파일 진행 중
- `--scale` CLI 코드 landed (L2725) 확인
- 그러나 기동 시 `--scale smoke` 미전달 → default `1b` 로 튐
- **후속 액션**: 바이너리 완성 후 명시적 `--scale smoke` 재발사 필요
