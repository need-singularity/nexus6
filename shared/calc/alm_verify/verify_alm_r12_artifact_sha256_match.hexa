// AUTO-GENERATED — shared/calc/alm_verify/generator.hexa
// manifest line id: alm_r12_artifact_sha256_match
// round: r12  gate: G_ARTIFACT  verifier_type: hash_match
// hyp: local vs R2 sha256 match
//
// Deterministic — LLM judge 금지.

let id = "alm_r12_artifact_sha256_match"
let target = "match"
let asset = "adapter_model.safetensors"

let pod = env("HEXA_VERIFY_POD")
let pod_host = if pod != "" { pod } else { "87xsc" }
// TODO (human tune): set local_path + remote_path explicitly if needed.
let local_cmd = "ssh " + pod_host + " 'sha256sum /workspace/" + asset + " 2>/dev/null | awk \"{print \\$1}\"'"
let remote_cmd = "ssh " + pod_host + " 'rclone sha256sum r2:anima-models/" + asset + " 2>/dev/null | awk \"{print \\$1}\"'"
let local_hash = exec(local_cmd).trim()
let remote_hash = exec(remote_cmd).trim()
let status = if local_hash != "" && local_hash == remote_hash { "PASS" } else { "FAIL" }
println("verify: " + id)
println("  asset:    " + asset)
println("  local:    " + local_hash)
println("  remote:   " + remote_hash)
println("  status:   " + status)
println("__VERIFY_RESULT__ " + id + " " + status + " " + local_hash + " vs " + remote_hash)
if status == "FAIL" { exit(1) }
