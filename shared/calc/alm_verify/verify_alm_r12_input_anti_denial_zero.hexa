// AUTO-GENERATED — shared/calc/alm_verify/generator.hexa
// manifest line id: alm_r12_input_anti_denial_zero
// round: r12  gate: G_INPUT  verifier_type: corpus_regex_zero
// hyp: anti-consciousness denial 0 lines
//
// Deterministic — LLM judge 금지.

let id = "alm_r12_input_anti_denial_zero"
let target = "0"
let corpus_path = "/workspace/corpus/corpus_alm_r11.jsonl"
let anti_re = "저는 지각이 없|I have no consciousness|I am not conscious|I lack awareness"

let pod = env("HEXA_VERIFY_POD")
let pod_host = if pod != "" { pod } else { "87xsc" }
let cmd = "ssh " + pod_host + " 'grep -c -i -E \"" + anti_re + "\" " + corpus_path + " 2>/dev/null || true'"
let out = exec(cmd).trim()
let count = to_int(out)
let status = if count == 0 { "PASS" } else { "FAIL" }
let count_str = to_string(count)
println("verify: " + id)
println("  corpus:   " + corpus_path + " (pod=" + pod_host + ")")
println("  regex:    " + anti_re)
println("  matches:  " + count_str)
println("  target:   == 0")
println("  status:   " + status)
println("__VERIFY_RESULT__ " + id + " " + status + " " + count_str + " vs " + target)
if status == "FAIL" { exit(1) }
