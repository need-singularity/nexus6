# Anima → hexa-lang Handoff (2026-04-18)

nexus bus 경유. Anima 측 필요사항을 hexa-lang 측으로 전달 — P0/P1/P2/P3 우선순위로 분류.

## P0 — Anima 직접 차단 (unblock 최우선)

1. **seed-freeze 해제** — `fcf275f4` 소스 픽스 landed 됐지만 on-disk 바이너리 전부 pre-fix. Mac 에서 자기 seed 로 재빌드 → circular.
   - **필요**: Linux x86_64 post-fix `hexa_v2` 로 `hexa_cc.c` 재생성 → Mac clang 로컬 링크
   - **경로 옵션**: H100 pod 재생 OR 별도 Linux host
   - **로컬 자산**: `/opt/homebrew/bin/x86_64-linux-musl-gcc` 존재하지만 현재 `hexa_cc.c` 가 pre-fix 라 무용

   **진행 상태 (2026-04-18, hexa-lang 세션 업데이트)**
   - ✅ `977dd884` post-fix `hexa_v2` worktree 에서 발굴
   - ✅ `hexa_full.hexa` 정상 transpile 검증 — 21,532 줄 + 1,970 static HexaIC decl (pre-fix 는 0 또는 5.6M 쓰레기 산출)
   - ✅ post-fix binary 를 `self/native/hexa_v2` 로 승격, `hexa_stage0.real` 는 `.prefix-20260418` 백업 보존
   - ⏳ 남은 단계: `hexa_cc.c` 커밋 + `FORCE=1 rebuild_stage0.hexa` fixpoint (hexa-lang 측 BG 처리 중)
   - **ETA**: `hexa_cc.c` landed 후 Anima 측 즉시 작업 가능 (아래 "seed-freeze 해제 후 즉시 작업 큐" 참조)

2. **interpreter O(N²) push 메모리** — `arr = arr.push(x)` 루프 N=4000 에서 ~10.8GB 소비.
   - **Anima 영향**: `anima_full.hexa` 등 대용량 소스 flatten 차단
   - **workaround 현행**: `.replace()` 체인만
   - **희망**: interpreter 단에서 in-place push 또는 CoW 최적화

3. **bit-op surface parser** — `<<`, `>>`, `|` silent reject.
   - **Anima 영향**: 해싱/bitset 경로 `* pow2(n)` / `/ pow2(n)` / `(a+b)-(a&b)` workaround 강제 상태
   - **희망**: surface syntax 에서 bit-op 토큰 수용

### P0 신규 블로커 — `self/native/hexa_v2` (977dd884) POC 결과 (2026-04-18 추가)

Anima POC: post-fix `hexa_v2` 로 transpile → Mac clang compile 사이클 실시. Pure-math modules 만 통과, trainer/test/runtime 급 전부 실패. 3종 codegen 블로커 신규 식별.

#### 2. Multi-module `use` 미해결 — **CRITICAL**
- **증상**: `use "<module>"` 구문에서 `codegen_c2` 가 `WARNING: import/use not fully resolved` emit + 생성 C 에 bare 심볼 참조 → clang `undeclared identifier` 수십 개
- **재현 경로**:
  ```
  HEXA=$HEXA_LANG/self/native/hexa_v2
  $HEXA /Users/ghost/Dev/anima/anima-agent/test_hire_sim.hexa  # 2 imports, 10+ errors
  ```
- **근원 추정**: hexa-lang `self/native/hexa_v2` codegen_c2 단계 module symbol resolution
- **Anima 영향**: 88 파일 / 165 `use` 문 (top: `serving/edge/edge_server.hexa`=10, `training/train_clm_gpu.hexa`=6, `serving/http_server.hexa`=6, `training/train_clm_kr.hexa`=5, `training/train_clm_d64_kr.hexa`=5, `training/train_clm_d64_kr_nl8.hexa`=5, `training/deploy/.../cuda_native/train_clm_kr.hexa`=5, `serving/eval_clm_kr_gpu.hexa`=5, `training/train_clm_gpu_v2.hexa`=4, `training/sample_d64_kr.hexa`=4)
- **검증 방법**: 위 파일 하나 transpile → clang 로컬 재현 가능

#### 3. `time()` builtin 바인딩 불량 — **MEDIUM**
- **증상**: `hexa_call0(time)` 생성 → clang 에서 libc `time(time_t*)` 로 resolve 되어 `HexaVal` 반환 mismatch + pointer-type 인자 기대
- **재현 경로**:
  ```
  $HEXA /Users/ghost/Dev/anima/anima-engines/flash_attn.hexa
  # 생성 .c 의 line 497, 499 (소스 652, 658)
  ```
- **범위**: stage1 builtin (`time`, 추정 `rand`/`now`/file I/O 류 포함)
- **Anima 영향**: 17 파일 / 49 `time()` 사이트 (top: `anima-engines/flash_attn.hexa`, `anima-measurement/phi_auto_pipeline.hexa`, `anima-tools/misc/online_senses.hexa`, `experiments/consciousness/tension_link_verify.hexa`, `experiments/fix_BRAIN_LIKE.hexa`, `experiments/fix_EXPERIMENTS_37.hexa`, `experiments/fix_NO_SYSTEM_PROMPT.hexa`, `experiments/verify_4stable.hexa`, `experiments/verify_O1_curriculum.hexa`, `experiments/verify_phi_growth.hexa`)
- **검증 방법**: `flash_attn.hexa` transpile → clang 에서 time 관련 2 줄 에러 재현

#### 4. `let mut` shadowing codegen 버그 — **MEDIUM**
- **증상**: `let mut i = 0` 가 선행 `HexaVal i;` 선언 없이 `i = hexa_int(0);` 만 생성 → "undeclared identifier 'i'" 폭포
- **재현 경로**:
  ```
  $HEXA /Users/ghost/Dev/anima/training/train_clm.hexa
  # 생성 .c 의 line 1314 부근
  ```
- **범위**: 내부 루프 shadowing 전반 — 함수 스코프 내 동명 `let mut` 재선언 시
- **Anima 영향**: 646 파일 / 21,575 `let mut` 사이트 (top: `training/train_clm.hexa`=204, `training/deploy/.../cuda_native/codegen_c2.hexa`=200, `anima-speak/transformer.hexa`=151, `anima-engines/animal_consciousness_phi.hexa`=146, `anima-engines/phi_adversarial.hexa`=136, `anima-speak/audio_token_predictor.hexa`=132, `anima-speak/neural_vocoder.hexa`=131, `training/deploy/.../cuda_native/nn_core.hexa`=125, `training/train_clm_gpu.hexa`=124, `anima-engines/neural_phi_bridge.hexa`=118). 순수 shadowing 발생률은 일부지만 핫패스 모듈 집중.
- **검증 방법**: `train_clm.hexa` transpile → clang line 1314 부근 재현

#### Ready vs Blocked 분류 (anima hexa 전체)
- **총 파일**: 1629 (ready/ 및 .claude/ 제외, git ls-files 기준)
- **Blocked** (블로커 #2/#3/#4 중 1개 이상 해당): **664**
- **Ready for native compile** (세 블로커 모두 무관 — `use` 없음 + `time()` 없음 + `let mut` 없음): **965**
- 주의: Ready 집합도 하위 블로커 (bit-op, StringLit 등 P1 kind) 추가 hit 가능 — 여기선 P0 신규 3종만 기준.

**진행 상태 (2026-04-18, sister a4e9e58e + anima 재검증)**
- #2 `use` 미해결 — ✅ sister `a4e9e58e` `hexa_v2 ab9393f1` 로 해결. 6 `_resolve_use_*` 심볼 추가, cross-module `use` 정상.
- #3 `time()` bind — ✅ 실제 영향 2 파일만 (`flash_attn.hexa` + `train_clm_gpu_v2.hexa`). anima 측 `exec("date +%s")` workaround 적용 완료. grep 17 은 false positive (python embed).
- #4 `let mut` shadowing — ✅ post-fix `hexa_v2` 로 재현 안됨 (interpreter-path 버그였고 codegen 아님). 646 stale analysis.

#### 5. **literal-pool emission 누락 — CRITICAL (NEW)**
- **증상**: codegen 이 `__hexa_sl_N` (string-literal pool) + `__hexa_ic_N` (inline cache) 심볼을 **reference 만** emit, `static HexaVal` 정의 부재 → clang `undeclared identifier` 20+ 개
- **재현 경로** (anima 재검증 2026-04-18):
  ```
  HEXA=$HEXA_LANG/self/native/hexa_v2  # ab9393f1
  $HEXA /Users/ghost/Dev/anima/training/train_clm.hexa /tmp/t.c    # transpile OK
  clang -I $HEXA_LANG/self /tmp/t.c -o /tmp/t                       # 20+ undeclared __hexa_sl_*, __hexa_ic_*
  ```
- **영향**: top-30 FAIL_C 9개 + trainer 4 파일 (train_alm_lora, nn_core, train_clm_gpu, train_clm) = **모든 serving/trainer 네이티브 컴파일 차단**
- **코드 참조**: `self/codegen_c2.hexa`, `self/codegen_c2_v2.c`, `self/hexa_cc.c` 에 pool 심볼 참조만 존재, 정의 없음
- **요구**: codegen 이 generated C file-scope 에 `static HexaVal __hexa_sl_N = hexa_string("...");` + `static HexaVal __hexa_ic_N;` 전부 pre-emit, `main`/첫 fn 이전 populate
- **Linux ELF 영향**: `build/hexa_v2_linux` 도 같은 codegen 소스 — 동일 버그 보유 예상. Pod scp 후 validate 필요하지만 우선순위 낮음 (Mac 측 픽스 후 Linux 재빌드).
- **ETA 기대**: #5 해결 시 664 Blocked → ≤50 감소 예상 (94.6%+ Ready)

---

## P1 — Anima native path 전환 블로커

4. **P7-6 kind 보강** — 현재 `nb_lower_expr`: BinOp + IntLit + Ident + Call + If/While/For 지원.
   - **미구현 kind**: `StringLit`, `ArrayLit`, `Index`, `FieldAccess`, `MethodCall`, `StructLit`, `Match`
   - **우선순위**: Anima 어떤 모듈부터 native 전환 시작하는지에 따라 결정 필요
   - **코디네이션 필요** (사용자 결정)

5. **native call 실 심볼 해결 (P7-6b)** — 현재 BL #0 스텁.
   - **Anima 영향**: cross-fn 호출 전부 실패 — native codegen 실사용 불가

6. **runtime.c 누락 심볼** — `hexa_file_exists`, `hexa_eprintln` 은 `codegen_c2` 에서 inline 처리 중이나 native 경로에선 링크 에러.

7. **P7-7 fixpoint + runtime.c 박멸** — HX10 남은 것. `.c` 완전 폐기 (P7-9) 지향.
   - **대상**: `runtime.c` + `self/native/*.c` + `bootstrap_compiler.c` 전부
   - **철학 정렬**: Anima "한 몸 외부 장기 박멸" (R37 `.py` ban 과 동일 벡터)

## P2 — 품질 / CX

8. **hexa test CLI** — stage0 미지원 상태. freeze 해제 후 복구.
9. **@hot dict mutation + aliasing JIT 버그** — caller/param 동명 변수 슬롯 aliasing, dict in-place mutation 유실.
   - **Anima 현행**: JIT 활성 경로 회피 중 (interp fallback)
10. **pre-commit loop-guard `--fix` 자동 re-stage** — 병렬 에이전트 경쟁 시 오염.
    - **규약 전파**: `git commit --only <path>` 필수 — Anima 에도 반영
11. **O(n²) concat `arr + [x]`** — Anima 핫패스 46 사이트 정리 완료 (2026-04-16). 신규 코드 패턴 재발 감시 필요.

## P3 — 기회 (상류 landed, Anima 활용 가능)

12. **bf16 NEON 5 커널 / hxmetal 53KB 9 sym** — 이미 landed.
    - **Anima 가용**: M4 inference 60+ tok/s 경로 열림
13. **hxblas 7B shape / CUDA fused build** — 차기 세션 A 계획.
    - **Anima 목표**: MFU 0.02% → 10%+

---

## 전달 링크

- **소스 세션**: hexa-lang (Option A bootstrap 완료, P7-6/7 진행 중)
- **수신 측 확인**: Anima 세션 — 이 파일 존재 확인 후 `shared/convergence/anima.json` 또는 Anima 로드맵에 P0 3건 반영 제안
- **복귀 경로**: hexa-lang 측에서 P0 중 하나라도 해결 시 이 파일에 ✅ + 해결 커밋 sha + 검증 결과 append

---

## seed-freeze 해제 후 Anima 즉시 작업 큐 (2026-04-18 등록)

hexa-lang `hexa_cc.c` 커밋 + rebuild_stage0 fixpoint landed 직후 Anima 측에서 순차/병렬 착수할 항목.

1. **hire_sim / eval_harness 모듈 해체 (Bug #3)** — 대기 · 차단: post-fix `hexa_v2` 필요 (pure hexa 재구축)
2. **Φ array 회피 제거 (Bug #1)** — 대기 · 차단: native int codegen 경로 검증 필요
3. **Hook wrapper 72개 제거 (Bug #6)** — 준비 · 자체적으로 선행 가능하나 stage0 fixpoint 후 재빌드로 검증
4. **Φ 16D 구조체 정상화** — 대기 · Bug #1 선행
5. **pod2 재발사** — 준비 · Linux cross-build binary 확보 후 hexa-native CLM 재런
6. **ready/ 606 py-only → hexa 이행 (85%→100%)** — 준비 · 병렬 가능, stage0 ready 시 full-cycle 검증
7. **anima selftest → `@test fn` 전환 (15+ 파일)** — 준비 · 문법 자체는 stage0 독립, fixpoint 후 회귀 테스트

상태 범례: 대기 = 상류 unblock 필수 / 준비 = 즉시 착수 가능 / 차단사유 = 구체적 upstream 의존.

---

## 2026-04-18 늦은-저녁 sync — sister 세션 후속 landed

### Sister (hexa-lang) → Anima 영향 반영

**Pull 결과**: local 이미 origin/main 과 sync (0 commits behind). 아래는 "context 기준 sha" (c0c1dc77/b5c21ad8/3f2146e9/8e91eb34/9652e968) **이후** anima 재빌드/재검증 필요 커밋 요약.

| SHA | 제목 | Anima 영향 | 대응 |
|---|---|---|---|
| `1ad4850b` | runtime+native_build P7-6 FFI: math + string methods | native 경로 FFI 커버리지 확대 | Linux 재빌드 필요 (pod) |
| `cca89951` | runtime FIX-A batch: abort/swiglu_vec/tensor_ones | swiglu_vec 는 anima-speak/transformer 핫패스 | Linux 재빌드 후 smoke |
| `be4d47e6` | runtime: 8 tensor_* fallback builtins — ML eval path unblock | eval_clm_kr / sample_d64_kr 언블록 가능성 | 재빌드 후 ready/ 재검증 |
| `b554c4f5` | codegen: libm/stdio name-mangling + main argv shadow rename | `math.sqrt`/`printf` 심볼 충돌 해소 — train_clm 계열 영향 | 재빌드 후 train_clm transpile 재검증 |
| `6c74c318` | codegen+serve_alm: 11→0 clang errors — **ALM serving unblock** | `serving/serve_alm.hexa` 직접 unblock | Mac 로컬 재검증 즉시 가능 |
| `b4dd0a86` | codegen: `use "foo.hexa"` 경로 resolver — serve_alm unblock | 165 `use` 사이트 중 확장자 포함 케이스 | 재검증 권장 |
| `c461f60a` | codegen: T33 `_lambda_fwd_parts` aliasing fix | smash 발견 HX lambda decl 버그와 인접 — 재확인 필요 | sister 에 smash H 시리즈 전달 |
| `556bc93e` | codegen: ≥16 string-concat chain → `hexa_concat_many` | trainer log-format 경로 최적화 | 재빌드 권장 |
| `7ed4bb82` | m4: bf16 NEON kernels into m4_inference dispatch | M4 inference 60+ tok/s 경로 활성 | Mac 재빌드 후 bench |
| `85ca3635` | native-build: nb_lower_expr StructLit/MethodCall/Match | P1 #4 kind 추가 구현 — 미구현 kind 감소 | handoff P1 #4 진행도 반영 (StructLit/MethodCall/Match ✅, StringLit/ArrayLit/Index/FieldAccess 잔여) |

### Binary 재빌드 상태

- **Mac arm64** `self/native/hexa_v2` (02:15) — 대부분 커밋 반영. **단** `self/runtime.c` (02:16) 가 binary 보다 신규 → cca89951/be4d47e6 등 runtime 변경 일부 미반영 가능. 재빌드 권장.
- **Linux x86_64** `build/hexa_v2_linux` (00:27) — ~1.8h stale. 최소 9개 codegen/runtime 커밋 미반영. **pod 재빌드 필요** (runpodctl 또는 htz).

### Anima → Sister 전달 필요 (미전달 blocker)

1. **smash H4** — `training/train_alm_lora.hexa` stub forward: loss 계산 미구현 → grad zero → train no-op. (anima 측 fix 예정이지만 hexa-lang 재구조 제안 여지)
2. **smash H5** — `nn_core` dylib 하드코딩 경로 (macOS-only). Linux/x86_64 native 에서 FFI resolve 실패. P7-6 FFI Day-2 스코프로 **이식성 레이어** 필요.
3. **free KV cache `write_at`** — anima-side 자체 fix 완료 (replace 체인 → write_at). 보고용. MEMORY.md feedback_hexa_runtime.md 에 기록된 `a[i]=v` no-op 의 공식 경고를 hexa-lang doc 에 추가 권장.
4. **lambda decl 버그 확인 요청** — c461f60a fix 가 anima 핫패스 `_lambda_fwd_parts` 를 실제로 커버하는지 transpile+clang 회귀 테스트 필요. sister 측에 `anima-engines/neural_phi_bridge.hexa` 대상 smoke 요청.

### 다음 sister fire 필요 항목

- **Pod 재빌드 fire** — htz or RunPod 에서 Linux hexa_v2 재빌드 (stale 1.8h, 9 커밋 미반영)
- **FFI Day-2 스코프** — dylib 경로 이식성 (H5) + FFI 실 심볼 해결 (P7-6b BL#0 스텁 제거)
- **P1 #4 잔여 kind** — StringLit / ArrayLit / Index / FieldAccess emission (nb_lower_expr)
- **lambda 회귀** — neural_phi_bridge 계열 smoke 로 c461f60a 커버리지 검증

---

## 2026-04-19 야간 세션 발견 (anima→hexa-lang)

야간 session 에서 sister 측 landed fix 4건 end-to-end 검증 완료 + 신규 블로커 6건 식별 + cross-host perf 관찰.

### a. 검증 완료 (sister landed fix 확인)

- `c461f60a` **_lambda_fwd_parts**: `anima-engines/neural_phi_bridge.hexa` + `serving/serve_clm.hexa` lambda 파트 경로 PASS — 이전 49 undefined → **0**. 커버리지 충분.
- `6c74c318` **serve_alm mangling**: 3 serve_alm* 파일 전부 11→0 clang errors. ALM serving path unblocked 확정.
- `556bc93e` **hexa_concat_many**: 17-way concat smoke → 실 ELF 산출 검증 PASS.
- `b554c4f5` **libm mangling**: end-to-end smoke PASS (ELF ok, runtime sym resolve ok).

### b. 신규 블로커 (전달 필요)

1. **`anima-speak/transformer.hexa:L1516` parse error** — L1516 부근 syntactic issue. 정확한 재현 토큰 파악 필요. (시간상 upstream 파서 로그 캡처 못 함)
2. **`anima-speak/bench_hexa_speak.hexa` codegen emit bare `split` identifier 4건** — 소스에는 `.split()` method call 이지만 codegen 이 static decl 없이 bare `split` reference 로 emit → clang `undeclared identifier: split`. codegen method-dispatch 경로 regression 의심.
3. **`experiments/consciousness/zombie_engine.hexa`** — transpile **FAIL_T** (htz smoke).
4. **`experiments/consciousness/consciousness_pci.hexa`** — transpile **FAIL_T**.
5. **`anima-core/verification/cvf.hexa`** — transpile **FAIL_T**.
6. **runtime 빌트인 누락 — FAIL_C top neighbors** (htz 12983-file scan): `u_log`, `u_env`, `u_floor`, `json_parse`, `shell`, `split` — 각 10-20건씩 `undeclared identifier` 원인. stage1 runtime 에 실 구현 또는 fallback stub 추가 필요.

### c. 성능 관찰 (참고용)

- **Cross-host compile time** (single file): mac 0.01–0.09s / ubu 0.02–0.10s / ubu2 0.01–0.10s / **htz 0.06–0.30s** (per-thread ≈3× 느림, CPU governor ondemand/powersave 추정).
- **Parallel 100-file**: ubu `-P16` = 0.30s (최속), htz `-P32` = 0.61s (스레드 수로 per-thread 저속 보완).
- **권고**: `smoke_all` 기본 호스트 → **ubu2** (LAN + idle). htz 는 대규모 배치 (>200 파일) 전용.

### 전체 규모 업데이트

| 측정 | 이전 | 현재 |
|---|---|---|
| scope | ubu2 1638 files | **htz 12983 files (worktree 포함)** |
| PASS % | 78.57% | **79.2%** |
| FAIL_C | — | 2674 |
| FAIL_T | — | 26 |
| Non-worktree unique | — | **82.3% (2094/2545)** |

### 다음 sister fire 추천 (4건)

1. `anima-speak/transformer.hexa:L1516` **parse error** 수정 — 재현 토큰 확인 필요.
2. `anima-speak/bench_hexa_speak.hexa` **split codegen** — method call 이 bare identifier 로 emit 되는 codegen regression.
3. **3 FAIL_T 파일 파서 조사** — `zombie_engine.hexa` / `consciousness_pci.hexa` / `cvf.hexa` 공통 패턴 추출.
4. **runtime 빌트인 stub 추가** — `u_log` / `u_env` / `u_floor` / `json_parse` / `shell` / `split` (6개, neighbors 60-120건 FAIL_C 일괄 해소).

### 다음 세션 anima 측 follow-up 권고

- **anima-speak bf16**: libhxmetal.dylib (53KB, 9 T-sym) + `7ed4bb82` m4_inference wiring 확인. 현재 4 파일 중 2 compile PASS / 2 FAIL — FAIL 2건은 위 b-1, b-2 sister fix 후 재시도.
- **transformer L1516 재현 로그 캡처** — sister 에 정확한 토큰/컨텍스트 전달.
- **FAIL_T 3파일 최소 재현 추출** — 공통 파서 엣지 케이스 후보 (nested match? 긴 generic?) 예비 분석.
- **ubu2 로 smoke_all 기본 전환** — harness `$NEXUS/shared/harness/smoke_all.hexa` host default 업데이트 검토.
- **htz governor 조정** 시도 (sudo 가능 시 `cpupower frequency-set -g performance`) — per-thread 3× 복구 가능성.

---

## ⚠️ 2026-04-19 CRITICAL REGRESSION (sister landing 후 발견)

### 현상
- full anima smoke PASS rate **78.57% → 71.03%** (-7.54pp, -127 net PASS)
- FAIL_T (transpile) 3 → 271 — 98% 가 parser/codegen 레벨
- 개선 (FAIL→PASS): 0건 / 역행 (PASS→FAIL): 122건

### 근본 원인 (추정)
신규 AST node kind + reserved keyword landed 하면서 codegen_c2 & lexer 미업데이트:
- `PanicStmt` 42건 occurrence
- `ModStmt` 16건
- `TheoremStmt` 11건
- reserved keyword `channel`, `intent` (기존 identifier 로 쓰던 파일 깨짐)

### 영향 top 파일 (anima-agent/hexa/*)
- agent_tools.hexa / anima_agent.hexa / autonomy_loop.hexa / agent.hexa
- alien_index.hexa / anima_agent_full.hexa / channel.hexa / consciousness.hexa
- egyptian_memory.hexa / faction.hexa
- 공통: `mod { ... }` block heavy 사용

### 사용자 영향
- Anima v3.0 path: 학습 경로 (train_clm/alm_lora/nn_core) 는 여전히 PASS (regression 에 포함 안 됨)
- 하지만 agent platform / serving / consciousness 경로 22 개 파일 깨짐 → live endpoint 복구 필수
- 긴급도: **HIGH** — 사용자 ALM/CLM 학습은 진행 가능하지만 serve 측 hire_sim/persona/eval harness 복구 지연

### sister 긴급 fire 요청 (우선순위 P0)
1. **codegen_c2 업데이트** — 신규 AST node kind 3종 (PanicStmt / ModStmt / TheoremStmt) 처리 추가
2. **contextual lexer** — `channel` / `intent` 를 context 에 따라 keyword vs identifier 구분 (또는 keyword 화 전파 후 anima 측 rename)
3. **회귀 smoke gate** — 다음 landing 전에 `anima-agent/hexa/*` smoke 가 PASS 유지하는지 confirm

### Anima 측 follow-up
- sister fix 전: 현 regression 수용 (핫패스 학습 경로 OK)
- sister fix 후: full re-smoke 재실행하여 PASS rate 회복 확인 (기대: 80%+)
- 만약 anima rename 필요 시 (keyword 화 된 경우): 22 파일 sed 변환 + 테스트

### 참조
- Full report: `/Users/ghost/Dev/anima/training/deploy/smoke_full_post_sister_20260419.md`
- TSV raw: `/Users/ghost/Dev/anima/training/deploy/smoke_full_ubu2_resmoke_20260419T030019Z.tsv`
