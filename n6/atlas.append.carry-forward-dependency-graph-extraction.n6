// ═══════════════════════════════════════════════════════════════
// atlas.append.carry-forward-dependency-graph-extraction.n6 — ω-cycle witness auto-ingest
// 작성: 2026-04-26, tool/omega_cycle_atlas_ingest.hexa (deterministic emit)
// 본체 충돌 방지: atlas.n6 직접 편집 대신 본 append 파일에만 기록
// repo: nexus | witness: /Users/ghost/core/nexus/design/kick/2026-04-27_carry-forward-dependency-graph-extraction_omega_cycle.json
// axes_total_surfaced: -1 | tier_1_count: 0
// 등급 정책 (a7 extended): [7] / [10*] / [10*PASS_LITERATURE] / [10*PROBE_RUN] / [10*BARRIER_CONFIRMED] / [10*NEGATIVE_VERDICT] / [11*REPO_INVARIANT]
// schema-guard: raw 23 (atlas DSL grammar mini-lint, exit 2 on violation)
// idempotence: same witness → byte-eq output (raw 68)
// ═══════════════════════════════════════════════════════════════

// ── facts auto-classified (@P/@C/@F/@L/@R/@S/@X via heuristic) ──

@C carry_forward_dependency_graph_extraction_topic_recovery_sibling_match_rationale_0 = A single snapshot is a moment; structural shape evolves. Tracking deltas (new edges, new chains) reveals carry-forward velocity per axis. :: omega_cycle_carry-forward-dependency-graph-extraction [7]
  => rationale of axis topic_recovery_sibling_match
  |> carry-forward-dependency-graph-extraction (auto-ingested ω-cycle)

@C carry_forward_dependency_graph_extraction_topic_recovery_sibling_match_rationale_1 = Today: 0 cycles, 0 orphans — clean. But the heuristic origin parser already saw 5+ format variants. As variants grow, structural drift will produce silent orphans unless lint enforced. :: omega_cycle_carry-forward-dependency-graph-extraction [7]
  => rationale of axis topic_recovery_sibling_match
  |> carry-forward-dependency-graph-extraction (auto-ingested ω-cycle)

@C carry_forward_dependency_graph_extraction_topic_recovery_sibling_match_rationale_2 = Heuristic parsers are fragile. A canonical schema reduces extraction ambiguity from O(n_patterns) to O(1). :: omega_cycle_carry-forward-dependency-graph-extraction [7]
  => rationale of axis topic_recovery_sibling_match
  |> carry-forward-dependency-graph-extraction (auto-ingested ω-cycle)

// EOF — total 3 facts auto-classified
