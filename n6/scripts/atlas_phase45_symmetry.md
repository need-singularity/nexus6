# atlas_phase45_symmetry.hexa — Phase 4.5 @S commute sweep

Purpose: retroactively emit the missing `@S` symmetry edges identified in Agent 24's audit (`shared/discovery/atlas_symmetry_audit_2026-04-11.md`). Scans `shared/n6/atlas.n6` read-only, parses every `{"type":"node"}` line whose `summary` matches `A op B = V` for commutative ops (`+`, `*`), buckets by `(op, rhs, sorted(A,B))` for commute pairs, and by `sorted(A,B)` across `+`/`*` for Vieta conjugacy. Emits JSONL edges `{"type":"edge","kind":"@S","label":"commute_*|commute_+|vieta_+_*",...}` to stdout. Usage: `hexa shared/n6/scripts/atlas_phase45_symmetry.hexa > shared/n6/atlas_phase45_symmetry.jsonl`. Fully regenerable — infra-only, atlas.n6 untouched, output file is gitignored (~280KB, 1427 edges as of 2026-04-11: 1402 commute + 25 vieta). Canonical `n*phi=12 ↔ phi*n=12` pair is included.
