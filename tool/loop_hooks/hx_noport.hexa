// L7_NOPORT — .hexanoport 마커 무결성 (프리젠 C 보호)
// 본체:
//   1. .hexanoport 마커 파일 전수 수집
//   2. 각 마커가 가리키는 C 파일 존재 + 보호 대상 확인
//   3. 포팅 실수 (C → .hexa 변환 시도) 감지 시 REJECT
fn main() {
    println("L7_NOPORT: hexa-lang noport invariant stub — not yet implemented")
}
