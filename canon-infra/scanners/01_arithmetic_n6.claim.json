{
  "id": "arithmetic.n6_identity",
  "type": "structural",
  "description": "sigma(6)*phi(6) == 6*tau(6) (= 12)",
  "actual_cmd": "python3 -c 'from math import gcd; sigma=lambda n:sum(i for i in range(1,n+1) if n%i==0); tau=lambda n:sum(1 for i in range(1,n+1) if n%i==0); phi=lambda n:sum(1 for k in range(1,n+1) if gcd(k,n)==1); print(int(sigma(6)*phi(6)==6*tau(6)))'",
  "expected": "1",
  "tolerance": 0,
  "must_fail": false,
  "recheck_cadence": "weekly",
  "owner": "arithmetic_core"
}
