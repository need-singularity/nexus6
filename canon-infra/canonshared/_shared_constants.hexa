// AUTO-GENERATED by scripts/gen_shared_constants.sh from
// nexus/n6/n6_core_constants.hexa — DO NOT EDIT BY HAND.
//
// Consumed by the 18 verify_*.hexa files via:
//     use "canonshared/_shared_constants"
// `tool/flatten_imports.hexa` inlines this file at preflight.
// Manual regen: NEXUS=/path/to/nexus bash scripts/gen_shared_constants.sh.
//
// Stage A (proposals/dup_derivation_consolidation_phase2_2026_04_24.md).

let SIGMA = 12      // sigma(6) = 1 + 2 + 3 + 6
let TAU   = 4       // tau(6)   = |{1, 2, 3, 6}|
let PHI   = 2       // phi(6)   = |{1, 5}| (Euler totient)
let N     = 6       // the candidate integer (sole >=2 hit for identity)
let SOPFR = 5       // sum-of-prime-factors-with-repetition: 2 + 3
let J2    = 24      // Jordan totient order 2, J_2(6) = sigma * phi = n * tau

// Lowercase aliases — backward-compat with verify_*.hexa files that inlined
// `let sigma = 12`, `let tau = 4`, ... (pre-Stage-A form).
let sigma = SIGMA
let tau   = TAU
let phi   = PHI
let n     = N
let sopfr = SOPFR
let j2    = J2
